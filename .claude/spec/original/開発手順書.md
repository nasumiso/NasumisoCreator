# LoRAによるイラストスタイル学習手順（完全版）

## 🎯 本手順書の対象者
- **開発者**: 学習環境の構築・スクリプト実行
- **なすみそ氏**: 画像提供・生成結果のフィードバック

---

## 🔧 事前準備

### 1. 必要な環境

#### **学習環境（開発者が用意）**
- **Google Colab**（推奨）
  - 無料版でも利用可能
  - Colab Pro（有料）ならより高速・安定
  - ブラウザだけで完結（Macへのインストール不要）
  - GPU（CUDA）が自動で利用できる

#### **生成環境（Windows10/11で動作）**
- AUTOMATIC1111 Stable Diffusion WebUI
- Python 3.10以上
- Git

💡 **重要**: Macでの学習は非推奨（CUDAなし・処理が遅い）
💡 画像生成はWindows環境での利用を想定

### 2. 自動インストールされるもの（Colab側）
- Stable Diffusion（SD 1.5またはSDXL）
- kohya_ss（LoRA学習ツール）
- Python 3.10+, Git, CUDAツールチェーン

---

## 📂 ステップ①：学習用画像の収集

### 🔹 画像の条件
- **枚数**: 10〜100枚程度
  - 少なすぎると学習不足
  - 多すぎると過学習のリスク
- **解像度**: 512x512推奨（後で自動リサイズ可能）
- **内容**: 同じキャラ・スタイルで統一
- **品質**: ぼやけていない、高解像度の画像

### 🔹 収集方法
1. なすみそ氏から画像を受領（Google Drive等で共有）
2. `projects/nasumiso_v1/raw_images/` に配置

```
projects/
└── nasumiso_v1/
    └── raw_images/
        ├── sketch_001.jpg
        ├── illust_02.png
        └── character_design.png
```

---

## 📂 ステップ②：画像の自動処理

### 🔹 目的
- 連番リネーム（管理しやすく）
- 512x512にリサイズ（学習効率向上）

### 🔹 実行方法

#### **スクリプトの配置**
`scripts/prepare_images.py` を作成（内容は開発者が用意）

#### **実行コマンド**
```bash
python scripts/prepare_images.py \
  --input projects/nasumiso_v1/raw_images \
  --output projects/nasumiso_v1/resized \
  --size 512
```

#### **出力結果**
```
projects/nasumiso_v1/resized/
├── img001.png
├── img002.png
├── img003.png
└── ...
```

💡 このステップで画像は自動的に整理されます

---

## 📂 ステップ③：自動タグ付け（キャプション生成）

### 🔹 目的
各画像に説明文（タグ）を付与し、学習精度を向上させる

### 🔹 使用ツール
- **WD14 Tagger**（推奨・高精度）
- または **DeepBooru**

### 🔹 実行方法

#### **オプション1: Colabで自動タグ付け**
```python
# Colab上で実行
!git clone https://github.com/toriato/stable-diffusion-webui-wd14-tagger
# WD14 Taggerを使って自動キャプション生成
```

#### **オプション2: ローカルスクリプト**
```bash
python scripts/auto_caption.py \
  --images projects/nasumiso_v1/resized \
  --output projects/nasumiso_v1/tags
```

#### **出力結果**
各画像に対応する `.txt` ファイルが生成される

```
projects/nasumiso_v1/tags/
├── img001.txt  → "1girl, red_hair, twintails, smile"
├── img002.txt  → "1boy, short_hair, casual_clothes"
└── ...
```

### 🔹 手動修正（任意）
- 自動生成タグが不正確な場合、テキストエディタで修正可能
- 重要なタグは先頭に配置すると効果的

---

## 📂 ステップ④：データセット整形

### 🔹 目的
Colab学習用に画像とタグを同じフォルダに配置

### 🔹 実行方法
```bash
python scripts/organize_dataset.py \
  --images projects/nasumiso_v1/resized \
  --tags projects/nasumiso_v1/tags \
  --output datasets/nasumiso_v1
```

### 🔹 最終的なデータセット構成
```
datasets/nasumiso_v1/
├── img001.png
├── img001.txt
├── img002.png
├── img002.txt
└── ...
```

💡 この形式がkohya_ssの標準入力フォーマット

---

## 📂 ステップ⑤：Google Colabで学習実行

### 🔹 準備

#### 1. Colabノートブックを開く
- `notebooks/train_lora_template.ipynb` をGoogle Colabで開く
- またはkohya_ss公式のColabノートブックを使用

#### 2. データセットをアップロード
```python
# Colab上で実行
from google.colab import drive
drive.mount('/content/drive')

# datasetsフォルダをGoogle Driveからコピー
!cp -r /content/drive/MyDrive/nasumiso_creator/datasets/nasumiso_v1 /content/datasets/
```

### 🔹 学習パラメータ設定（推奨値）

```python
# LoRA学習の基本設定
config = {
    "pretrained_model": "runwayml/stable-diffusion-v1-5",  # ベースモデル
    "output_dir": "./lora_output",                          # 出力先
    "train_data_dir": "./datasets/nasumiso_v1",             # データセット
    "resolution": "512,512",                                 # 解像度
    "train_batch_size": 1,                                   # バッチサイズ
    "learning_rate": 1e-4,                                   # 学習率
    "max_train_steps": 2000,                                 # ステップ数
    "save_every_n_epochs": 1,                                # 保存間隔
    "mixed_precision": "fp16",                               # 混合精度
    "network_dim": 32,                                       # LoRAのランク
    "network_alpha": 16                                      # LoRAのアルファ値
}
```

### 🔹 学習実行
```python
# Colab上で実行
!python train_network.py \
  --pretrained_model_name_or_path={config["pretrained_model"]} \
  --train_data_dir={config["train_data_dir"]} \
  --output_dir={config["output_dir"]} \
  --resolution={config["resolution"]} \
  --learning_rate={config["learning_rate"]} \
  --max_train_steps={config["max_train_steps"]} \
  --network_dim={config["network_dim"]} \
  --network_alpha={config["network_alpha"]}
```

### 🔹 学習時間の目安
- 50枚、2000ステップ: 約20〜30分（Colab Pro）
- 100枚、3000ステップ: 約40〜60分

### 🔹 出力ファイルのダウンロード
```python
# 学習完了後、.safetensorsファイルをダウンロード
from google.colab import files
files.download('./lora_output/nasumiso_v1.safetensors')
```

💾 `nasumiso_v1.safetensors` がLoRAモデルファイルです

---

## 📂 ステップ⑥：Windows環境での画像生成

### 🔹 環境構築（初回のみ）

#### 1. AUTOMATIC1111 WebUIのインストール
```bash
# Windows PowerShellまたはコマンドプロンプトで実行
git clone https://github.com/AUTOMATIC1111/stable-diffusion-webui.git
cd stable-diffusion-webui
webui-user.bat
```

初回起動時に必要なファイルが自動ダウンロードされます（数GB）

#### 2. LoRAモデルの配置
```
stable-diffusion-webui/
└── models/
    └── Lora/
        └── nasumiso_v1.safetensors  ← ここに配置
```

### 🔹 画像生成の実行

#### 1. WebUIの起動
```bash
cd stable-diffusion-webui
webui-user.bat
```

ブラウザで `http://localhost:7860` が開きます

#### 2. プロンプト入力例
```
Positive prompt:
1girl, red_hair, twintails, smile, casual_clothes, standing, white_background
<lora:nasumiso_v1:0.8>

Negative prompt:
lowres, bad_anatomy, bad_hands, text, error, missing_fingers
```

💡 `<lora:nasumiso_v1:0.8>` の数値（0.8）でLoRAの強さを調整
- 0.5〜0.7: 控えめ
- 0.8〜1.0: 標準
- 1.0以上: 強め（過学習リスク）

#### 3. 生成パラメータ（推奨）
- **Sampling method**: DPM++ 2M Karras
- **Steps**: 20〜30
- **CFG Scale**: 7〜9
- **Size**: 512x512

#### 4. 生成結果の保存
- 自動的に `outputs/txt2img-images/` に保存されます
- 気に入った画像は `outputs/test_generations/` にコピーして管理

---

## 📂 ステップ⑦：品質チェックと再学習

### 🔹 評価ポイント
- ✅ なすみそ氏のスタイルが再現できているか
- ✅ 過学習（同じ構図ばかり）になっていないか
- ✅ プロンプトへの反応が適切か

### 🔹 改善方法

#### **過学習の場合**
- 学習ステップ数を減らす（2000 → 1500）
- LoRAのランクを下げる（32 → 16）

#### **学習不足の場合**
- 学習ステップ数を増やす（2000 → 3000）
- 学習率を上げる（1e-4 → 2e-4）

#### **データ追加**
- なすみそ氏から追加画像を受領
- ステップ①から再実行

---

## 📊 プロジェクト管理

### 🔹 複数キャラ/スタイル対応
```
projects/
├── nasumiso_v1/          # なすみそスタイル（ver1）
├── nasumiso_v2/          # 改良版
└── another_character/    # 別キャラクター
```

各プロジェクトで独立した学習が可能

### 🔹 バージョン管理
```
projects/nasumiso_v1/lora_model/
├── nasumiso_v1_epoch1.safetensors
├── nasumiso_v1_epoch2.safetensors
└── nasumiso_v1_final.safetensors
```

エポックごとに保存して比較すると最適なモデルが見つかります

---

## 🎉 完了！

これで「なすみそクリエイター」の基本的なワークフローが完成しました。

### 📌 次のステップ（フェーズ2以降）
- GUI化（スクリプト実行を簡単に）
- SDXL対応（より高品質な生成）
- 表情差分の自動生成機能
- スタンプ/グッズ用の自動レイアウト機能

---

## 📞 トラブルシューティング

### Q. Colabで学習が途中で止まる
A. 無料版は使用時間制限があります。Colab Proへのアップグレードを検討してください。

### Q. 生成画像がぼやける
A. CFG Scaleを上げる（7→9）、またはSampling Stepsを増やす（20→30）

### Q. LoRAが効きすぎる/効かない
A. プロンプト内の `<lora:nasumiso_v1:0.8>` の数値を調整してください。

### Q. Windows環境でWebUIが起動しない
A. Python 3.10.6がインストールされているか確認してください。